const options = {
  filter: false,
  print: false,
  rowsPerPage: 10,
  rowsPerPageOptions: [10],
  download: false,
  search: false,
  selectableRows: false,
  pagination: true,
};

export default options;
